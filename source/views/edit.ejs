<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.ejs')  %>
</head>

<body>
    <nav>
        <a href="/productos/">Volver a lista</a>
    </nav>
    <form action="/productos/actualizar?m=PUT" method="post">
        <fieldset>
            <legend>Nombre del producto</legend>
            <input type="text" name="name" id="name" value="<%= product.name %> ">
        </fieldset>
        <fieldset>
            <legend>Precio</legend>
            <input type="text" name="price" id="price" value="<%= product.price %>">
        </fieldset>
        <fieldset>
            <legend>Categoría</legend>
            <input type=" radio " name="category" id="category-cat " value="cat" <%= product.category == req.body.category ? "checked" : "" %>  >
            <label for="category-cat ">Cat</label>
            <input type="radio " name="category" id="category-dog " value="dog" <%= product.category == req.body.category ? "checked" : "" %>>
            <label for="category-dog ">Dog</label>
            <input type="radio " name="category" id="category-fish " value="fish" <%= product.category == req.body.category ? "checked" : "" %>>
            <label for="category-fish ">Fish</label>
        </fieldset>
        <button type="submit ">Guardar</button>

    </form>
</body>

</html>